describe("User can see pannel stats", () => {
  // beforeEach(() => {
  //   cy.viewport(1450, 1000);
  //   cy.server();
  //   cy.requests();
  // })

  // it("can successfully open pannel", ()=> {    
  //   cy.login();
  //   cy.get('#pannel-arrow').click()

  //   cy.get('.stats-grid').should('contain', 3)
  // })
})